stages:
  - test
  - codestyle
  - messdetector
  - stats
  - stage
  - deploy

# PHP Docblock Checker
test:docblock:
  stage: codestyle
  before_script:
    - sh ./dev/build_dev_env.sh
    - sh ./dev/build_dev_env_docblock.sh
  script:
  - ./binaries/php-docblock-checker/phpdoccheck -d ./lib --no-interaction
  only:
    - branches
  except:
    - master

test:unit_7.0.18:
  stage: test
  before_script:
    - sh ./dev/build_dev_env.sh
    - sh ./dev/build_dev_env_phpunit.sh
  coverage: '/^\s*Lines:\s*\d+.\d+\%/'
  script:
  - php ./binaries/phpunit.phar -c phpunit.xml

# This tool check syntax of PHP files faster than serial check with fancier output.
test:lint:
  stage: codestyle
  before_script:
    - sh ./dev/build_dev_env.sh
  script:
  - ./bundle/vendor/bin/parallel-lint --exclude library --exclude vendor --exclude binaries --exclude bundle/vendor --blame -e php .
  only:
    - branches
  except:
    - master
    - develop

# PHP_CodeSniffer tokenizes PHP, JavaScript and CSS files and detects violations of a defined set of coding standard
test:phpcs:
  stage: codestyle
  before_script:
    - sh ./dev/build_dev_env.sh
  script:
    - bash ./dev/phpcs_cbf.sh
  only:
    - develop

# A tool for quickly measuring the size of a PHP project.
test:phploc:
  stage: stats
  before_script:
    - sh ./dev/build_dev_env.sh
    - cp /home/gitlab-runner/tools/phploc.phar ./binaries/
    - chmod +x ./binaries/phploc.phar
  script:
    - ./binaries/phploc.phar lib
  only:
    - develop

# Run our tests
test:dependancies:
  stage: stats
  before_script:
    - sh ./dev/build_dev_env.sh
  script:
  - php ./dev/dependCheck.php
  only:
  - develop

# PHP Mess Detector (PHPMD)
test:phpmd_cleancode_develop:
  stage: messdetector
  before_script:
    - sh ./dev/build_dev_env.sh
    - cp /home/gitlab-runner/tools/phpmd.phar ./binaries/
    - chmod +x ./binaries/phpmd.phar
  script:
  - ./binaries/phpmd.phar ./lib text cleancode
  only:
    - develop

# PHP Mess Detector (PHPMD)
test:phpmd_codesize_develop:
  stage: messdetector
  before_script:
    - sh ./dev/build_dev_env.sh
    - cp /home/gitlab-runner/tools/phpmd.phar ./binaries/
    - chmod +x ./binaries/phpmd.phar
  script:
  - ./binaries/phpmd.phar ./lib text codesize
  only:
    - develop

# PHP Mess Detector (PHPMD)
test:phpmd_controversial_develop:
  stage: messdetector
  before_script:
    - sh ./dev/build_dev_env.sh
    - cp /home/gitlab-runner/tools/phpmd.phar ./binaries/
    - chmod +x ./binaries/phpmd.phar
  script:
  - ./binaries/phpmd.phar ./lib text controversial
  only:
    - develop

# PHP Mess Detector (PHPMD)
test:phpmd_design_develop:
  stage: messdetector
  before_script:
    - sh ./dev/build_dev_env.sh
    - cp /home/gitlab-runner/tools/phpmd.phar ./binaries/
    - chmod +x ./binaries/phpmd.phar
  script:
  - ./binaries/phpmd.phar ./lib text design
  only:
    - develop

# PHP Mess Detector (PHPMD)
test:phpmd_naming_develop:
  stage: messdetector
  before_script:
    - sh ./dev/build_dev_env.sh
    - cp /home/gitlab-runner/tools/phpmd.phar ./binaries/
    - chmod +x ./binaries/phpmd.phar
  script:
  - ./binaries/phpmd.phar ./lib text naming
  only:
    - develop

# PHP Mess Detector (PHPMD)
test:phpmd_unusedcode_develop:
  stage: messdetector
  before_script:
    - sh ./dev/build_dev_env.sh
    - cp /home/gitlab-runner/tools/phpmd.phar ./binaries/
    - chmod +x ./binaries/phpmd.phar
  script:
  - ./binaries/phpmd.phar ./lib text unusedcode
  only:
    - develop

# PHP Copy/Paste Detector (PHPCPD)
test:phpcpd_develop:
  stage: codestyle
  before_script:
    - sh ./dev/build_dev_env.sh
    - cp /home/gitlab-runner/tools/phpcpd.phar ./binaries/
    - chmod +x ./binaries/phpcpd.phar
  script:
  - ./binaries/phpcpd.phar ./lib
  only:
    - develop

# PHP Mess Detector (PHPMD)
test:phpmd_cleancode_flow:
  stage: messdetector
  before_script:
    - sh ./dev/build_dev_env.sh
    - cp /home/gitlab-runner/tools/phpmd.phar ./binaries/
    - chmod +x ./binaries/phpmd.phar
  script:
  - ./binaries/phpmd.phar ./lib text cleancode
  only:
    - branches
  except:
    - master
    - develop
  allow_failure: true

# PHP Mess Detector (PHPMD)
test:phpmd_codesize_flow:
  stage: messdetector
  before_script:
    - sh ./dev/build_dev_env.sh
    - cp /home/gitlab-runner/tools/phpmd.phar ./binaries/
    - chmod +x ./binaries/phpmd.phar
  script:
  - ./binaries/phpmd.phar ./lib text codesize
  only:
    - branches
  except:
    - master
    - develop
  allow_failure: true

# PHP Mess Detector (PHPMD)
test:phpmd_controversial_flow:
  stage: messdetector
  before_script:
    - sh ./dev/build_dev_env.sh
    - cp /home/gitlab-runner/tools/phpmd.phar ./binaries/
    - chmod +x ./binaries/phpmd.phar
  script:
  - ./binaries/phpmd.phar ./lib text controversial
  only:
    - branches
  except:
    - master
    - develop
  allow_failure: true

# PHP Mess Detector (PHPMD)
test:phpmd_design_flow:
  stage: messdetector
  before_script:
    - sh ./dev/build_dev_env.sh
    - cp /home/gitlab-runner/tools/phpmd.phar ./binaries/
    - chmod +x ./binaries/phpmd.phar
  script:
  - ./binaries/phpmd.phar ./lib text design
  only:
    - branches
  except:
    - master
    - develop
  allow_failure: true

# PHP Mess Detector (PHPMD)
test:phpmd_naming_flow:
  stage: messdetector
  before_script:
    - sh ./dev/build_dev_env.sh
    - cp /home/gitlab-runner/tools/phpmd.phar ./binaries/
    - chmod +x ./binaries/phpmd.phar
  script:
  - ./binaries/phpmd.phar ./lib text naming
  only:
    - branches
  except:
    - master
    - develop
  allow_failure: true

# PHP Mess Detector (PHPMD)
test:phpmd_unusedcode_flow:
  stage: messdetector
  before_script:
    - sh ./dev/build_dev_env.sh
    - cp /home/gitlab-runner/tools/phpmd.phar ./binaries/
    - chmod +x ./binaries/phpmd.phar
  script:
  - ./binaries/phpmd.phar ./lib text unusedcode
  only:
    - branches
  except:
    - master
    - develop
  allow_failure: true

# PHP Copy/Paste Detector (PHPCPD)
test:phpcpd_flow:
  stage: codestyle
  before_script:
    - sh ./dev/build_dev_env.sh
    - cp /home/gitlab-runner/tools/phpcpd.phar ./binaries/
    - chmod +x ./binaries/phpcpd.phar
  script:
  - ./binaries/phpcpd.phar ./lib
  only:
    - branches
  except:
    - master
    - develop
  allow_failure: true

deploy_review:
  stage: stage
  script:
    - echo "Deploy a review app"
    - wget -O /tmp/deploy https://nxfifteen.me.uk/gitlab/nx-fitness/nxfitness-core/raw/$CI_COMMIT_REF_NAME/dev/deploy_review
    - bash /tmp/deploy "/home/gitlab-runner/review.nxfitness-core" $CI_COMMIT_REF_NAME $CI_ENVIRONMENT_SLUG
    - rm -f /tmp/deploy
  environment:
    name: review/$CI_COMMIT_REF_NAME
    url: http://docs.core.itsabeta.nx/$CI_COMMIT_REF_NAME
  only:
    - develop

deploy_staging:
  stage: stage
  script:
    - echo "Deploy to staging server"
    - wget -O /tmp/deploy https://nxfifteen.me.uk/gitlab/nx-fitness/nxfitness-core/raw/$CI_COMMIT_REF_NAME/dev/deploy_staging
    - bash /tmp/deploy "/var/www/dev/core/staging" $CI_COMMIT_REF_NAME $CI_ENVIRONMENT_SLUG
    - rm -f /tmp/deploy
  environment:
    name: staging
    url: http://core.itsabeta.nx/$CI_ENVIRONMENT_SLUG/ux/
  only:
    - develop

deploy_prod:
  stage: deploy
  script:
    - echo "Deploy to production server"
    - eval $(ssh-agent -s)
    - ssh-add /home/gitlab-runner/id_rsa_nxcore
    - scp ./dev/deploy_prod nxad@nxfifteen.me.uk:/home/nxad/homes/gitrepos/deploy_prod
    - ssh nxad@nxfifteen.me.uk chmod 755 /home/nxad/homes/gitrepos/deploy_prod
    - ssh nxad@nxfifteen.me.uk /home/nxad/homes/gitrepos/deploy_prod
    - ssh-add -D
    - ssh-agent -k
    - unset SSH_AGENT_PID
    - unset SSH_AUTH_SOCK
  environment:
    name: production
    url: https://nxfifteen.me.uk/api/fitbit/
  only:
  - master
