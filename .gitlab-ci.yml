# Select image from https://hub.docker.com/r/_/php/
image: php:7.1.4-alpine

before_script:
# Install dependencies
- bash ci/docker_install.sh > /dev/null
- php /usr/local/bin/composer.phar install

test:phploc:
  script:
  - php /usr/local/bin/phploc inc
